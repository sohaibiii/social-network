import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  featureWrapperStyle: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    marginRight: 10,
    marginTop: 5
  },
  checkIconStyle: { marginRight: 5 }
});

export default styles;
