import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  loaderStyle: {
    width: "100%",
    height: "100%",
    justifyContent: "center",
    alignItems: "center"
  }
});
