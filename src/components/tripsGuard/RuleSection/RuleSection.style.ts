import { StyleSheet } from "react-native";

const ruleSectionStyle = StyleSheet.create({
  containerStyle: {
    marginBottom: 5,
    flexDirection: "row",
    alignItems: "center",
    padding: 5
  },
  textStyle: {
    marginHorizontal: 10,
    flex: 1
  },
  row: {
    flexDirection: "row"
  }
});

export default ruleSectionStyle;
